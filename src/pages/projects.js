import * as React from 'react';
import Layout from '../components/layout';

const ProjectsPage = (props) => {
  return <Layout pageTitle="Proyectos" {...props}></Layout>;
};

export default ProjectsPage;
